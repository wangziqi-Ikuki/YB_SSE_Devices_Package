# 电导工站与合成工站开发总结

面向 Uni-Lab-OS 固态电解质实验线，本设备包把合作方现场工站接入 Uni-Lab-OS：Uni-Lab-OS 作为网口客户端下发动作、订阅状态；工站软件作为服务端执行。两套工站各 1 份设备图、各 1 组端口，可同时部署，不改 Uni-Lab-OS 内核。

| 项 | 电导工站 | 合成工站 | 合计 |
|---|---|---|---|
| 已注册动作 | 23 | 24 | 47 |
| 状态通道 | 16 | 16 | 32 |
| 现场机构 | 7 | 9 | 16 |
| 协议错误码 | 10（0–9） | 14（0–13） | — |
| 自动化测试用例 | 31 | 28 | 59 |
| 真机默认端口 | 8091 | 8092（占位，上线前确认） | — |
| 虚拟机端口 / 交互页 | 19091 / 19092 | 19101 / 19102 | 可同时开 |

通信约定一致：统一编码结构化报文、回车换行分帧、请求—响应；连接超时默认 5 秒，等待响应默认 10 秒，单条报文上限约 4 兆字节。查询失败可重连重试 1 次；写操作不自动重发，避免重复执行。

---

## 一、电导率自动化测试工站

### 内容

把「备料 → 压片 → 交流阻抗 / 电子电导 → 出料」做成可编排、可回读的平台设备。

- **整批自动跑**：1 次「启动整批」，工站连续完成 13 步（拆模、测厚、转移、扫码加粉、组装、交流阻抗、电子电导、出料到托盘），工作流不必再串联 13 个分步节点。
- **手动单步**：另有 13 个具名动作，供联调、复测、排障。
- **查询与结果**：6 个查询动作（工站健康、物料占位、批次进度、批次结果、按月查批次、连通探测）。样品结果回传约 15 项，含离子电导、电子电导、厚度两次、面积、温度、配方、组分、料瓶码、电阻等。
- **物料系统展示桌**：漏斗 / 烧结料瓶 / 模具共 3 层，每层 2×5 共 10 格，合计 30 格占位可镜像；现场操作屏 1–10 号对应甲 01–甲 05、乙 01–乙 05。
- **状态通道 16 路**：工站四态（空闲 / 运行中 / 故障 / 离线）、连通、7 路机构在线、3 路库位余量（各 0–10）、是否在跑、当前步骤（1–13，无批次为 0）、完成数、失败数。健康通道约 2 秒刷新，余量约 5 秒，库位主动轮询默认 30 秒（可关）。
- **离线可验**：独立虚拟机与交互页，可点 30 格库位、拨 7 路设备、看 13 步进度。演示备料为 3 瓶 + 3 模 + 3 漏斗。自动化测试 31 条覆盖驱动、注册表与展示桌。

### 效果

| 场景 | 量化结果 |
|---|---|
| 日常实验 | 人工备料后 1 次启动，最多按库位连续测完当批样品；进度按 13 步、完成数 / 失败数实时可见 |
| 工作流编排 | 整批路径从 13 个分步节点降为 1 个启动节点；分步仅在排障时使用 |
| 数据回收 | 单样品约 15 项结果回平台，可按批次号或按月（年-月）回溯 |
| 看板 | 30 格库位 + 7 路机构 + 4 种工站健康，不必盯现场操作屏 |
| 联调 | 桌面虚拟机先跑通；真机只改地址与端口（常见 8091）。空库位启动会报错码 2，已有批次再启为码 4 |

### 价值

把电导表征从「人盯工站点按钮」变成「平台下单、工站执行、结果回传」。整批 1 键相对 13 步串联，编排成本按节点数约降一个数量级；30 格占位与约 15 项结果对齐后续材料筛选。虚拟机把联调从现场搬到桌面，真机切换只改地址，缩短上线周期。

---

## 二、合成工站

### 内容

把「配方上传 → 合成任务（加样 / 声共振 / 装瓶）→ 后处理任务（烧结下料）」做成可编排的平台设备。本阶段物料系统展示桌只做背景与状态镜像，不建槽位。

- **下单 14 个写动作**：上传配方、创建合成任务、启动任务、上坩埚、关方舱门、加样确认、启动加样、声共振启停与下料、扫码装瓶、启动烧结、焦耳热下料、马弗炉下料。
- **查询 10 个读动作**：合成任务队列、后处理队列、工站机构、按生产批次查配方与分烧、按装瓶码反查生产批次、上坩埚 / 加样 / 声共振 / 烧结状态、连通探测。读失败返回完整响应不抛错；写失败（结果码非 0）由平台报错。
- **配方录入**：4 列基本类型（物料名称、重量克、加样精度克、是否预加），前端逐项可填。演示配方 4 种物料。托盘按类型最多 4 / 5 / 6 槽。坩埚规格 5 种，烧结路径 5 条（4 台马弗炉 + 1 路焦耳热）。
- **状态通道 16 路**：工站四态、连通、9 路机构（其中 3 路仅在线/离线，6 路为离线/空闲/运行/完成）、未完成合成任务数、未完成后处理数、当前任务号与后处理号。健康约 2 秒刷新，任务计数约 5 秒，状态轮询默认 30 秒（可关）。
- **过程状态机**：合成任务 6 态（未开始→完成），后处理 8 态（未开始→入库完成），生产批次 8 态（等待加样→已完成）。协议错误码 14 个，覆盖控制器未连、配方重名、槽位越界、任务冲突等。
- **离线可验**：独立虚拟机（与电导端口不冲突），演示预装 1 条配方、1 条合成任务、1 条后处理、1 个生产批次。自动化测试 28 条。

真机典型链路约 12 步：上传配方 → 创建任务 → 启动任务 → 上坩埚 →（方舱时关外门）→ 加样确认 → 启动加样 → 声共振三步 → 扫码装瓶 → 启动烧结 → 下料。虚拟机默认自动推进时，创建任务后会跳过「启动任务」，真机仍必须发启动。

### 效果

| 场景 | 量化结果 |
|---|---|
| 配方落地 | 平台 4 列对齐填写后 1 次上传；按托盘最多 6 槽一次下发合成任务，不必在现场软件手抄组分 |
| 过程可视 | 合成 6 态 + 后处理 8 态 + 生产批次 8 态可查；装瓶码可反查生产批次 |
| 后处理 | 5 条烧结路径纳入同一工作流，合成与后处理不断档 |
| 排障 | 14 个错误码直接对应现场原因（如码 4 配方重名、码 5 槽位越界、码 8 已有任务在跑） |
| 联调 | 虚拟机 1 套演示数据即可走通查询链；真机端口 8092 仅为占位，上线前向合作方确认 |

### 价值

把固态电解质合成从「现场下单软件操作」提升为「平台配方驱动、任务可追溯」。配方、生产批次、装瓶码一条链，可与电导工站的料瓶扫码、约 15 项测试结果对上。14 个错误码减少联调猜错；4 列配方表避免对象列表在前端展不开。

---

## 三、合起来的价值

两条工站覆盖固态电解质线的**合成制备**与**电导表征**，协议与接入方式一致，现场各维护 1 份设备图。

- **内容**：47 个平台动作、32 路状态通道、2 套虚拟机、59 条自动化测试、2 张物料系统展示桌（电导 30 格实体库位；合成本阶段仅背景与状态镜像）。
- **效果**：桌面可完整走通下单与回读，真机只改地址；两站 4 个端口互不占用，可并行联调。电导整批从 13 节点降为 1 节点；合成写操作 14 步覆盖加样到烧结下料。
- **价值**：Uni-Lab-OS 成为实验编排与数据入口，工站成为执行端。闭环路径为「配方 → 合成生产批次 → 装瓶码 → 电导扫码入测 → 约 15 项电导结果」。减少现场值守、缩短表征反馈、为后续自动筛选留接口。
