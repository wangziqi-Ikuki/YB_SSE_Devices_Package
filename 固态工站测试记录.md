# Uni-Lab 新内核（kernel-v2）固态电解质合成工站测试记录

体例对齐仓库内 [电导率工站新内核测试记录.md](电导率工站新内核测试记录.md)，以及飞书工站记录页
<https://dptechnology.feishu.cn/wiki/ZwuYwS7lsiUupkk8XXDczXYtnGd>。
工作区加载方式见 [Uni-Lab OS 工作区说明书](http://115.190.137.109:30184/workspace.html)。

## 测试信息

- 时间：2026 年 9 月 17 日（配方 / TASK 链）；2026 年 9 月 18 日（`start_sintering`）
- 地点：本机虚拟联调（合成工站 Mock TCP），未上宜宾真机
- 内核分支：`product/durable-scheduler-kernel-v2`，Uni-Lab OS 0.11.4
- 虚拟环境：`unilabv2`（Python 3.11.14，miniforge3）
- 设备包：`D:\unilabostest\YB_SSE_Devices_Package`，分支 `test/kernel-v2`（提交 `fb35bb5`）
- 参考文档：[工作区](http://115.190.137.109:30184/workspace.html)、[合成工站使用说明.md](合成工站使用说明.md)、[合成工站下单软件接口.md](合成工站下单软件接口.md)、飞书工站记录 [ZwuYwS7lsiUupkk8XXDczXYtnGd](https://dptechnology.feishu.cn/wiki/ZwuYwS7lsiUupkk8XXDczXYtnGd)
- 被测对象：合成工站设备单动作（DeviceActionRun），对接本机虚拟工站（Mock TCP `127.0.0.1:19101`）

合成工站当前没有登记「整批合成」工作流（`package.yaml` 里只有电导的 `start_batch`）。
新内核把设备包 + 启动图作为一个工作区加载后，单动作由后端派发给 edge，不再从命令行直连工站。
因此本次链路是：

```
启动图 synthesis-dry-run.json（synthesis_station_01）
  → workspace start 加载工作区（runtime-mode normal）
  → POST /api/v1/device-action-runs 提交单动作
  → edge 调用 SynthesisStation 动作
  → 虚拟工站执行 TCP 协议（upload_recipe / create_task / start_task / 查询 / start_sintering）
```

## 一、代码与配置修改及原因

### 1. 现有 `dry-run.json` 只有电导工站，不能用来联调合成

`deployment/graphs/dry-run.json` 只声明了 `conductivity_station_01`，指向电导 Mock `127.0.0.1:19091`。
按说明书「启动图负责创建实例」，合成动作必须绑到启动图里的稳定实例 ID，不能猜类型名。

本次新增独立启动图 `deployment/graphs/synthesis-dry-run.json`，与电导图分离，避免两个工站互相抢连接：

```json
{
  "metadata": {
    "purpose": "simulation",
    "owner": "yb-sse-lab",
    "version": "0.1.0",
    "notes": "合成工站虚拟联调：连接本机 Mock TCP 127.0.0.1:19101。"
  },
  "nodes": [
    {
      "id": "synthesis_station_01",
      "name": "合成工站",
      "type": "device",
      "class": "community.yb_sse_devices.synthesis_station",
      "parent": null,
      "position": { "x": 0.0, "y": 0.0, "z": 0.0 },
      "config": {
        "ip": "127.0.0.1",
        "port": 19101,
        "use_mock": false
      },
      "data": {}
    }
  ],
  "links": []
}
```

要点：

- `class` 使用检查结果中的完整类型名 `community.yb_sse_devices.synthesis_station`，不要手写猜测
- `id` 是工作流 / 单动作绑定的实例 ID：`synthesis_station_01`
- `use_mock: false`：虚拟机由独立终端启动，进程内 Mock 没有交互页

### 2. 单动作走 DeviceActionRun，而不是发布工作流

电导测试走的是「工作流源码 → package.yaml 登记 → 发布 → POST /workflow-tasks」。
合成工站本次验证的是典型联调链上的多个动作，尚未写成一条工作流，因此使用官方单动作入口：

```
POST $API/device-action-runs
```

请求字段与 Backend DTO 同名：`material_uuid`（设备物料身份）、
`workflow_node_template_uuid`（动作节点模板）、`param`、`idempotency_key`。
创建后得到标准工作流任务（`execution_kind=ad_hoc_device_action`）和唯一 Job，再按任务 UUID 跟踪。

设备物料 UUID、动作模板 UUID 每次工作区启动后从接口现取，不要写死到脚本以外的生产配置里：

```powershell
GET  $API/materials
GET  $API/workflow-node-templates?page=1&page_size=50
```

本次启动后：

| 用途 | 值 |
| --- | --- |
| 设备物料 | `synthesis_station_01` → `76e7323a-2ec2-5d93-8713-0ee87b3d6fb0` |
| 资源模板 | `6d482954-1389-52f7-8f82-43ed4c049f0a` |
| `upload_recipe` | `ca68e8a0-9fa7-5353-a0f4-b51d9f84545e` |
| `create_task` | `3a6fa32d-7121-5731-acb9-123bfc3cecbf` |
| `start_task` | `b702ee62-e932-549a-b2b4-534c91ff1657` |
| `query_tasks` | `fef2d9a1-61ad-568c-be4a-5dbb71ed6684` |
| `query_posts` | `64787180-11ee-5135-ac36-fb711a30dded` |
| `query_lot` | `40752e5b-5e12-5b1d-99d6-7cdf54d5f4d7` |
| `query_bottle_code` | `0ab5f253-c4e7-5b80-95ce-be830479fbcd` |
| `start_sintering` | `0e050596-056d-5386-842e-b17257dc388e` |
| `get_sintering_status` | `6d24b0e4-3f8e-5453-bb63-f53b351c38e8` |

## 二、启动与测试提交

### 步骤 1：启动虚拟合成工站

```powershell
conda activate unilabv2
cd D:\unilabostest\YB_SSE_Devices_Package
python -m yb_sse_devices.synthesis_mock_server --host 127.0.0.1 --port 19101 --ui-port 19102
```

TCP 监听 `127.0.0.1:19101`，交互页 `http://127.0.0.1:19102/`。
与电导虚拟机（`19091` / `19092`）端口不同，可以同时开。

本次未加 `--demo`。交互页随后可见演示配方 `260708-Li6PS5Cl-R1` 与 `POST-000198`；
联调使用新上传配方 `260917-Li6PS5Cl-R1`，不复用演示配方名，避免 `result=4`（配方名称重复）。

### 步骤 2：启动工作区

```powershell
unilab workspace start --workspace . `
  --graph deployment/graphs/synthesis-dry-run.json `
  --runtime-mode normal --startup-mode develop --wait 300
unilab workspace status --workspace . --json
```

关键返回：

```
phase succeeded
backend  ready  http://127.0.0.1:56753
edge     ready
plc      idle
renderer idle
workflowProgress {'loaded': 1, 'total': 1}   workflowRuntimePhase ready
```

`--runtime-mode normal` 才会真正建立到工站的 TCP 连接；`dry-run` 只验证包发现与加载，
不连接真实驱动。**backend 地址每次启动都会变**，后续 REST 调用的 API 根必须从
`workspace status` 现取：

```powershell
$API = (unilab workspace status --workspace . --json | ConvertFrom-Json).components.backend.address + "/api/v1"
```

在线设备确认 `synthesis_station_01` 已注册，`transport=edge-control`。
UniLab 作为 TCP 客户端连上 Mock（`client_count=1`）。

### 步骤 3：上传配方 `upload_recipe`

```powershell
POST $API/device-action-runs
```

参数（四列物料按顺序对齐，与使用说明演示组分一致）：

| 参数 | 值 |
| --- | --- |
| `recipe_name` | `260917-Li6PS5Cl-R1` |
| `formula` | `Li6PS5Cl` |
| `synthesis_mass` | `3.86` |
| `n_ball_bead` | `80` |
| `powder_names` | `Li2S`, `LiBr`, `LiCl`, `P2S5` |
| `powder_weights` | `0.9`, `0.87`, `1.2`, `0.88` |
| `powder_tolerances` | `0.0007`, `0.0005`, `0.001`, `0.0005` |
| `powder_pre_adds` | 否、是、否、否 |

返回：

```
task_uuid   e078e3b0-1600-4f86-b4a4-dbc0ebf36ebe
status      succeeded
job         4c48caed-f3c5-4e5b-9db6-9f20f1b1de4c  succeeded
return      {"request_id": 115, "result": 0}
```

Mock 配方清单变为 `260708-Li6PS5Cl-R1`、`260917-Li6PS5Cl-R1`。

### 步骤 4：下发并启动 TASK

`create_task`（托盘类型 1 = 4 槽，坩埚类型 1，槽位 1/2 都绑刚上传的配方）：

```
task_uuid   dae0f257-61b8-47a7-83ed-f9cc1880ac50
status      succeeded
return      {"request_id": 293, "result": 0, "task_id": "TASK-000169",
             "data": {"task_id": "TASK-000169"}}
```

`create_task` 之后 TASK 为未开始，必须再发 `start_task`。
`start_task` 参数 `task_id=TASK-000169`：

```
task_uuid   be51db90-d34d-424a-9632-bcb561879513
status      succeeded
return      {"request_id": 312, "result": 0, "task_id": "TASK-000169"}
```

虚拟机默认自动推进：`start_task` 后数秒内 TASK 走完并生成 POST。
本次未关自动推进，因此没有停在上坩埚 / 加样 / 声共振等中间工步。

### 步骤 5：查询 TASK / POST / LOT / 装瓶码

| 动作 | UniLab 任务状态 | 工站 `result` | 要点 |
| --- | --- | --- | --- |
| `query_tasks` | succeeded | 0 | `TASK-000169`，`task_state=5`（完成） |
| `query_posts` | succeeded | 0 | 新后处理 `POST-000199` |
| `query_lot` | succeeded | 0 | `LOT-260917-002`，`lot_state=7`，已装瓶 |
| `query_bottle_code` | succeeded | 0 | 新装瓶码可反查回该 LOT |

### 步骤 6：启动烧结 `start_sintering`（2026 年 9 月 18 日）

9 月 17 日自动推进后的新 POST 已到完成态，不能再烧结。
本日复用已在跑的演示 POST `POST-000198`（`post_state=6`，装瓶入库，仍属运行中）。

Mock 在 17:01 已监听 `127.0.0.1:19101`，交互页 `http://127.0.0.1:19102/`，状态：

```
recipe_names    260708-Li6PS5Cl-R1
task_count      1
post_count      1
running_posts   POST-000198
auto_advance    true
```

演示 POST 不在 `post_started` 里，不会被自动推进到 `post_state=7`。按 [工作区说明书](http://115.190.137.109:30184/workspace.html) 验证门四重新加载工作区：

```powershell
unilab workspace start --workspace . `
  --graph deployment/graphs/synthesis-dry-run.json `
  --runtime-mode normal --startup-mode develop --wait 300
unilab workspace status --workspace . --json
```

关键返回：

```
phase succeeded
backend  ready  http://127.0.0.1:50049
edge     ready
plc      idle
renderer idle
workflowProgress {'loaded': 1, 'total': 1}   workflowRuntimePhase ready
host     ready  http://127.0.0.1:50045
```

UniLab 作为 TCP 客户端连上 Mock（`client_count=1`，`127.0.0.1:55510`）。
设备物料 UUID 与 9 月 17 日相同（`76e7323a-2ec2-5d93-8713-0ee87b3d6fb0`），
`start_sintering` / `get_sintering_status` 模板 UUID 本次现取，见上表。

`start_sintering` 参数按 [合成工站下单软件接口.md](合成工站下单软件接口.md) 4.1：

| 参数 | 值 | 含义 |
| --- | --- | --- |
| `post_id` | `POST-000198` | 工站分配的 POST，不手造 |
| `joule_heating_fetch_mode` | `auto` | 焦耳热自动下坩埚 |

```
task_uuid   33e936a0-be88-488b-95f8-2ac125263b0b
status      succeeded
job         85f1af55-e4b3-4c67-bc24-ae03c1445775  succeeded
return      {"request_id": 70, "result": 0, "post_id": "POST-000198"}
```

随后用 `get_sintering_status` 回读马弗炉 / 焦耳热：

```
task_uuid   3ab545c1-3278-4f88-858a-62693887e5e2
status      succeeded
job         b591d20d-0c5b-4deb-b187-c28306de956a  succeeded
return      request_id=77  result=0  post_id=POST-000198
```

## 三、结果返回

### 设备单动作结果

本次联调链上的写动作与查询动作全部 `succeeded`，工站协议码均为 `result: 0`。
`result: 0` 是工站协议成功码；`task_id` / `post_id` / `lotId` 由工站分配，不手填。

### 虚拟工站侧任务结果

```
配方        260917-Li6PS5Cl-R1    化学式 Li6PS5Cl    合成总量 3.86 g    球磨珠 80
TASK        TASK-000169           task_state=5（完成）
POST        POST-000199           由 TASK 自动推进后生成
槽位 1      LOT-260917-002        大坩埚 CRU-L-006    小坩埚 CRU-S-008
槽位 2      LOT-260917-003        大坩埚 CRU-L-007
装瓶码      LOT-260917-002-CRU-S-008-260917
LOT 状态    lot_state=7           bottled=true
```

`query_lot` 回读配方实称与预加标记，与上传参数一致：

| 物料 | 目标重量 g | 精度 g | 预加 | 回读实称 g |
| --- | --- | --- | --- | --- |
| Li2S | 0.9 | 0.0007 | 否 | 0.8999 |
| LiBr | 0.87 | 0.0005 | 是 | 0.87 |
| LiCl | 1.2 | 0.001 | 否 | 1.1999 |
| P2S5 | 0.88 | 0.0005 | 否 | 0.8799 |

`query_bottle_code` 用新装瓶码反查：

```
lotId       LOT-260917-002
recipe_name 260917-Li6PS5Cl-R1
cubic       CRU-L-006
bottled     true
small_cubic CRU-S-008    send_state=2    fetch_state=2
```

交互页同步显示配方数、TASK / POST 计数和最近一条 TCP 动作。
Mock 自动推进完成后，`running_tasks` 为空，`running_posts` 含 `POST-000199`（以及原有演示 `POST-000198`）。

### 烧结回读（9 月 18 日）

`start_sintering` 与 `get_sintering_status` 均 `succeeded`，工站协议码 `result: 0`。
驱动把 `post_id` 回写到动作结果，便于后续 `fetch_furnace` / `fetch_joule_heating` 连线。

演示 LOT `LOT-260813-001` 上有两个小坩埚，对应两种烧结方式：

| 小坩埚 | `firing_type` | 设备 | 上料 | 烧结 | 下料 | 冷却 |
| --- | --- | --- | --- | --- | --- | --- |
| `CRU-S-001` | 0（马弗炉 1） | `furnace_id=1` | `upload_state=2` 成功 | `sintering_state=1` 进行中 | `fetch_state=0` 等待 | — |
| `CRU-S-005` | 4（焦耳热） | `joule_heating` | `upload_state=2` 成功 | `sintering_state=2` 完成 | `fetch_state=2` 成功 | `cooling_state=2` 完成 |

这与演示数据一致：`CRU-S-001` 已上料、烧结未结束；`CRU-S-005` 已烧结并下料。
`joule_heating_fetch_mode=auto` 时，焦耳热不需要再发 `fetch_joule_heating`。
马弗炉要等上料成功且烧结完成后才能 `fetch_furnace`；本次 `sintering_state=1`，未发下料。

Mock 交互页最近动作先是 `start_sintering result=0`，随后 `get_sintering_status result=0`。
`running_posts` 仍为 `POST-000198`，`client_count=1`。

### 与电导工站测试的差异

| 项 | 电导（9 月 16 日） | 合成（本次） |
| --- | --- | --- |
| 启动图 | `deployment/graphs/dry-run.json` | `deployment/graphs/synthesis-dry-run.json` |
| 设备实例 | `conductivity_station_01` | `synthesis_station_01` |
| 执行入口 | 发布工作流后 `POST /workflow-tasks` | `POST /device-action-runs` |
| 虚拟机 | `19091` / `19092` | `19101` / `19102` |
| 工站产物 | `batch_id` | `task_id` / `post_id` / `lotId` / 装瓶码 / 烧结状态 |

## 四、复位与停止

```powershell
unilab workspace stop --workspace . --wait 300 --json
```

虚拟工站在交互页点「初始化 / 清零」可清空配方、TASK 与 POST；重复测试前建议先清零。
虚拟工站默认自动推进（对应 TASK 完成后生成 POST）。
只在需要手动单步验证真机链路时，才加 `--no-auto-advance` 或取消勾选「自动推进」。

真机逐步链路。虚拟联调已覆盖配方 / TASK / 查询，以及烧结启动与状态回读；
中间工步（上坩埚、关方舱、确认预加、加样、声共振、扫码装瓶）和马弗炉下料尚未逐步点过：

```
upload_recipe → create_task → start_task → upload_cubic
  →（方舱时 close_cabin_outer_door）→ confirm_recipe → start_recipt
  → start_acoustic_resonance → fetch_acoustic_resonance → finish_acoustic_resonance
  → scan_big_cubic_to_bottle → start_sintering → fetch_furnace / fetch_joule_heating
```

`start_sintering` 约束：POST 必须存在且 `post_state < 7`，否则 `result=9`；
找不到 POST 为 `result=7`，PLC 离线为 `result=1`，参数错误为 `result=2`。

## 五、上真机前的注意事项

- 启动图 `ip` / `port` 改为合作方工站地址；驱动默认端口 **8092 只是占位**，上线前向合作方确认
- 不要开 `use_mock`，不要连 `19101`
- `runtime-mode` 用 `normal`；`dry-run` 不连接真实驱动，通过 dry-run 不代表真机验收通过
- 真机必须发 `start_task`；Mock 自动推进 TASK→POST 仅为仿真便利
- `start_sintering` 必须在 POST 完成前下发；`post_state=7` 会返回 `result=9`
- 只验证烧结时可用 `--demo` 预装 `POST-000198`，不要依赖自动推进刚生成的 POST
- 配方名称必须唯一，重复上传返回 `result=4`
- 真机首跑前确认联锁、急停、取消与异常恢复路径
- 生产地址与密钥不得写入设备包，由运行环境注入

## 附：本次使用的稳定身份

| 名称 | 值 |
| --- | --- |
| 类型命名空间 | `community.yb_sse_devices` |
| 设备类型 | `community.yb_sse_devices.synthesis_station` |
| 设备实例 | `synthesis_station_01` |
| 设备物料 UUID | `76e7323a-2ec2-5d93-8713-0ee87b3d6fb0` |
| 9 月 17 日配方 | `260917-Li6PS5Cl-R1` |
| 9 月 17 日 TASK | `TASK-000169` |
| 9 月 17 日 POST | `POST-000199` |
| 9 月 17 日 LOT | `LOT-260917-002`、`LOT-260917-003` |
| 9 月 17 日装瓶码 | `LOT-260917-002-CRU-S-008-260917` |
| 上传配方任务 UUID | `e078e3b0-1600-4f86-b4a4-dbc0ebf36ebe` |
| 下发 TASK 任务 UUID | `dae0f257-61b8-47a7-83ed-f9cc1880ac50` |
| 启动 TASK 任务 UUID | `be51db90-d34d-424a-9632-bcb561879513` |
| 烧结演示配方 | `260708-Li6PS5Cl-R1` |
| 烧结演示 POST | `POST-000198` |
| 烧结演示 LOT | `LOT-260813-001` |
| 启动烧结任务 UUID | `33e936a0-be88-488b-95f8-2ac125263b0b` |
| 查询烧结任务 UUID | `3ab545c1-3278-4f88-858a-62693887e5e2` |

## 六、复跑：启动烧结 `start_sintering`（2026 年 9 月 18 日 17:47）

本段是在工作区与 Mock 都已停干净后，按同一方案重跑，不改前面 9 月 17 日配方 / TASK 记录，也不覆盖上文第一次烧结结果。

### 启动

```powershell
python -m yb_sse_devices.synthesis_mock_server --host 127.0.0.1 --port 19101 --ui-port 19102 --demo
unilab workspace start --workspace . `
  --graph deployment/graphs/synthesis-dry-run.json `
  --runtime-mode normal --startup-mode develop --wait 300
```

```
Mock          TCP 127.0.0.1:19101   交互页 http://127.0.0.1:19102/
              --demo 预装 POST-000198（post_state=6，仍在运行）
workspace     phase succeeded
backend       ready  http://127.0.0.1:56761
edge          ready
host          ready  http://127.0.0.1:56756
client_count  1
```

设备物料与动作模板与上午一致（工作区重启后现取，值未变）：

| 用途 | 值 |
| --- | --- |
| 设备物料 | `76e7323a-2ec2-5d93-8713-0ee87b3d6fb0` |
| `start_sintering` | `0e050596-056d-5386-842e-b17257dc388e` |
| `get_sintering_status` | `6d24b0e4-3f8e-5453-bb63-f53b351c38e8` |

### 提交与回读

`start_sintering`：`post_id=POST-000198`，`joule_heating_fetch_mode=auto`

```
task_uuid   2e964331-047e-458e-9d17-2be90b818d01
status      succeeded
job         1097626b-696b-49d4-86ae-3ecb3e4ee33f  succeeded
return      {"request_id": 25, "result": 0, "post_id": "POST-000198"}
```

`get_sintering_status`：

```
task_uuid   95bb2e44-b010-4981-a6cb-56d11354b7ce
status      succeeded
job         cb2613a1-d63e-4802-b223-8011d76fc2a0  succeeded
return      request_id=29  result=0  post_id=POST-000198
```

| 小坩埚 | 设备 | 上料 | 烧结 | 下料 | 冷却 |
| --- | --- | --- | --- | --- | --- |
| `CRU-S-001` | 马弗炉 1 | 2 成功 | 1 进行中 | 0 等待 | — |
| `CRU-S-005` | 焦耳热 | 2 成功 | 2 完成 | 2 成功 | 2 完成 |

与第一次烧结回读一致。Mock 最近动作为 `get_sintering_status result=0`（17:47:23），`running_posts` 仍为 `POST-000198`。

### 复跑身份

| 名称 | 值 |
| --- | --- |
| 本次 backend | `http://127.0.0.1:56761` |
| 启动烧结任务 UUID | `2e964331-047e-458e-9d17-2be90b818d01` |
| 查询烧结任务 UUID | `95bb2e44-b010-4981-a6cb-56d11354b7ce` |
