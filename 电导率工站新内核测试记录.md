# Uni-Lab 新内核（kernel-v2）固态电解质电导工站测试说明

## 测试信息

- 时间：2026 年 9 月 16 日
- 地点：宜宾实验室 - 常规理化实验室 - 电导率测试工站
- 内核分支：`product/durable-scheduler-kernel-v2`，Uni-Lab OS 0.11.4
- 虚拟环境：`unilabv2`（Python 3.11.14，miniforge3）
- 设备包：`D:\Uni-Lab-OS-V2\YB_SSE_Devices_Package`，分支 `test/kernel-v2`（基线提交 `c493bd7`）
- 参考文档：<http://115.190.137.109:30184/workspace.html>
- 被测对象：电导工站单节点工作流 `start_batch`，对接本机虚拟工站（Mock TCP）

新内核把「设备包 + 启动图 + 工作流清单」作为一个工作区整体加载，动作不再由命令行直接下发，
而是由后端把工作流任务派发给 edge。因此本次测试的链路是：

```
工作流源码（start_batch.py）
  → package.yaml 登记 → package inspect 编译包目录（PackageCatalog）
  → workspace start 加载工作区 → 工作流发布（publish）
  → 提交工作流任务（workflow-task）→ edge 调用设备动作 → 虚拟工站执行 1–13 步
```

## 一、代码与配置修改及原因

### 1. 动作结果合同必须是同文件内声明的扁平 TypedDict（关键阻塞项）

首次执行 `unilab package inspect` 直接失败：

```
[ERROR] 包目录（PackageCatalog）编译失败：action_contract_invalid
```

用 AST 扫描器复现后，诊断定位到唯一一处：

```
DIAG conductivity_station start_batch typed
{'code': 'invalid_action_result', 'path': '/return',
 'message': 'Action 定义不符合 Workflow 版本 1 合同'}
```

原因是 `ConductivityStation.start_batch` 的返回类型 `StartBatchResponse` 声明在单独的
`yb_sse_devices/conductivity_types.py` 中。新内核的动作合同校验（`action_contract_schema.py`
→ `action_result_schema.py`）是**纯静态 AST 解析、不导入作者代码**的，它只在声明该动作的
**同一个源文件内**查找返回类型的类定义；跨文件引用无法解析，一律判为无效结果合同。

同一套校验还有两条限制，一并记录，避免后续设备再踩：

| 限制 | 说明 |
| --- | --- |
| 类基不得带关键字 | `class X(TypedDict, total=False)` 会被拒绝，只能是 `class X(TypedDict)` |
| 字段不支持嵌套结构 | 只接受 `str` / `int` / `float` / `bool` / `Literal` / `ResourceSlot` / `list[...]` / `Optional[...]` 等扁平标量，不能再嵌套 TypedDict |
| 返回类型必须同文件 | 跨模块 import 的 TypedDict 无法通过静态解析 |

因此把结果类型内联到 `yb_sse_devices/conductivity.py`，并把原来的三层嵌套
（`request_id` / `result` / `data.batch_id` / `message`）拍平为两个字段：

```python
class StartBatchResult(TypedDict):
    """动作结果合同只接受同文件内声明的扁平 TypedDict。"""

    result: int
    batch_id: str


@action(description="启动整批自动实验，工站连续执行 1–13 步，无需再串联分步")
def start_batch(self) -> StartBatchResult:
    response = self._require_ok(self._request("start_batch"))
    self._invalidate_batch_cache()
    data = response.get("data")
    batch_id = data.get("batch_id") if isinstance(data, dict) else ""
    return {
        "result": int(response.get("result") or 0),
        "batch_id": str(batch_id or ""),
    }
```

TCP 协议报文本身没有改动，扁平化只发生在驱动返回给 UniLab 的动作结果上。

配套改动：

- 删除 `yb_sse_devices/conductivity_types.py`（内联后已无引用）
- 工作流 `yb_sse_devices/workflows/start_batch.py`：`started.data.batch_id` → `started.batch_id`
- `tests/test_conductivity_station.py` 三处断言：`["data"]["batch_id"]` → `["batch_id"]`

### 2. 工作区身份与启动文件（按说明书「工作区」章节对齐）

| 文件 | 内容与原因 |
| --- | --- |
| `pyproject.toml` | 发布名改为 `yb-sse-devices`，与 Python 包 `yb_sse_devices` 及类型命名空间 `community.yb_sse_devices` 一致；`dependencies` 加入 `unilabos`；新增 `[tool.unilabos.startup]` 指定默认启动图与本地配置 |
| `package.yaml` | 登记允许加载的工作流。位于 `workflows/` 的文件不会自动加载，必须逐项登记，且 UUID 要与源码装饰器完全一致 |
| `deployment/graphs/dry-run.json` | 声明设备实例 `conductivity_station_01`，`class` 为 `community.yb_sse_devices.conductivity_station`（从 inspect 结果复制，不要手写猜测），`config` 指向虚拟工站 `127.0.0.1:19091`，`use_mock: false`（Mock 由独立终端启动） |
| `deployment/local_config.py` | 本地非敏感配置，`ak`/`sk` 留空，关闭浏览器自动打开与更新反馈 |
| `.gitignore` | 追加 `.unilabos/`，避免运行时目录进入版本库 |

`package.yaml` 内容：

```yaml
package:
  name: yb_sse_devices

workflows:
  - workflow_uuid: 3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196
    source: yb_sse_devices/workflows/start_batch.py
```

### 3. 单节点工作流

`yb_sse_devices/workflows/start_batch.py` 只有一个执行节点，调用工站的 `start_batch`，
由工站自己连续跑完 1–13 步，不在工作流里串联分步动作：

```python
class StartConductivityBatchResult(TypedDict):
    result: int
    batch_id: str


conductivity_station: ConductivityStation = device("conductivity_station_01")


@workflow(
    workflow_uuid="3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196",
    displayname="电导工站启动整批",
    description="备料完成后调用 start_batch，工站连续执行 1–13 步并返回工站响应。",
)
def start_conductivity_batch() -> StartConductivityBatchResult:
    # unilab:node_uuid=9d2e6c11-4a83-4f70-b1c5-0e8a37d5f4a2
    started = conductivity_station.start_batch()
    return {
        "result": started.result,
        "batch_id": started.batch_id,
    }
```

要点：

- `device("conductivity_station_01")` 绑定的是**启动图中的实例 ID**，不是设备类型 ID
- 每个执行节点前的 `# unilab:node_uuid=...` 注释是该节点的稳定身份，不要随意改动
- 工作流返回类型同样要求是本文件内声明的扁平 TypedDict

## 二、启动与测试提交

### 步骤 1：启动虚拟工站并备料

```powershell
conda activate unilabv2
cd D:\Uni-Lab-OS-V2\YB_SSE_Devices_Package
python -m yb_sse_devices.mock_server --host 127.0.0.1 --port 19091
```

TCP 监听 `127.0.0.1:19091`，交互页 `http://127.0.0.1:19092/`。
在交互页点击「装入演示 3+3+3」完成备料，确认瓶 / 模 / 漏斗各 3 个且数量对齐：

```
bottle : 1110000000
mold   : 0001110000
funnel : 0000001110
七路设备在线：7
```

备料是工序 0 的人工动作，不是 TCP 动作。若库位为空或三层数量不一致，
工站会返回 `result=2`，工作流节点将失败。

### 步骤 2：安装设备包并检查

```powershell
python -m pip install -e .
unilab package inspect --path . --out ./dist/inspect
```

通过后的输出：

```
package inspect 完成：yb-sse-devices@0.1.0
  class_namespace : community.yb_sse_devices
  设备来源        : 包目录（PackageCatalog）
  设备数          : 2 (conductivity_station, synthesis_station)
  资源投影数      : 7
  归档            : dist\inspect\yb_sse_devices-0.1.0.tar.gz
```

### 步骤 3：启动工作区

```powershell
unilab workspace start --workspace . `
  --graph deployment/graphs/dry-run.json `
  --runtime-mode normal --startup-mode develop --wait 300
unilab workspace status --workspace . --json
```

关键返回：

```
phase succeeded
backend  ready  http://127.0.0.1:51591
edge     ready
plc      idle
renderer idle
workflowProgress {'loaded': 1, 'total': 1}   workflowRuntimePhase ready
```

`--runtime-mode normal` 才会真正建立到工站的 TCP 连接；`dry-run` 只验证包发现与加载，
不连接真实驱动。**backend 地址每次启动都会变**，后续 REST 调用的 API 根必须从
`workspace status` 现取，不要写死端口：

```powershell
$API = (unilab workspace status --workspace . --json | ConvertFrom-Json).components.backend.address + "/api/v1"
```

### 步骤 4：发布工作流

新加载的工作流初始状态是 `source`，**不发布无法运行**，这一步容易漏掉。

```powershell
# 先看创作态修订号
GET  $API/workflows/3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196/authoring
#   → authoring state applied, revision 2

# 按该修订号发布
POST $API/workflows/3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196/publications
Body {"revision": 2}
```

返回 `code: 0`，工作流状态由 `source` 变为 `published`，revision 2；
输出合同 `output_contract.outputs` 为 `result` 与 `batch_id` 两项，与驱动扁平化后的
动作结果一致。

### 步骤 5：运行前预检

```powershell
GET $API/workflows/3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196/run-preflight?run_mode=normal
```

```json
{
  "status": "runnable_now",
  "can_run": true,
  "summary": {
    "execution_node_count": 1,
    "passed_check_count": 1,
    "blocking_check_count": 0,
    "deferred_check_count": 3
  }
}
```

3 项 deferred（设备实例选择、设备状态复检、资源准入）属于正常设计：
这些检查在派发时刻原子复核，不阻塞提交。

### 步骤 6：提交工作流任务

```powershell
POST $API/workflow-tasks
Body {"workflow_uuid": "3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196", "run_mode": "normal"}
```

返回：

```
task_uuid 21efa6b9-6753-461f-af5b-14531995d3dc
status    running
```

跟踪：

```powershell
GET $API/workflow-tasks/21efa6b9-6753-461f-af5b-14531995d3dc
GET $API/workflow-tasks/21efa6b9-6753-461f-af5b-14531995d3dc/jobs
```

## 三、结果返回

### 工作流任务结果

```
task status  succeeded
outputs      {"batch_id": "20260916123404106", "result": 0}
jobs         2 个 job 全部 succeeded
```

`result: 0` 是工站协议成功码，`batch_id` 由工站在 `start_batch` 时生成。

### 虚拟工站侧批次结果

```
batch       20260916123404106
state       completed
running     False
sample      3           （3 个样品全部走完）
step        13          （最后一步：测试物料转移至托盘）
finished    3
failed      0
last_action batch_status result=0
```

三个样品依次完成 1–13 步，无失败样品。交互页同步显示 13 步进度与最近一条 TCP 动作。
批次明细（电阻 R、厚度 h1/h2、面积、离子电导率）可通过 `batch_result` 动作按批次号查询，
离子电导率按 `σ = d / (R × A) × 1000`（mS/cm）计算，圆片面积 A = π × (0.5 cm)²。

### 单元测试

```powershell
python -m pytest tests -q
# 58 passed, 3 failed
```

3 项失败与本次改动无关，已用 `git stash` 回退到改动前复跑确认为既有问题：

| 失败用例 | 原因 |
| --- | --- |
| `test_post_init_does_not_block_when_real_station_ignores_writes` | `_read_frame` 对非 JSON 响应持续 recv 直到连接关闭，抛 `TransportError` 而非用例期望的 `ProtocolError` |
| `test_invalid_responses_are_rejected[{not-json}]` | 同上，分帧容错行为与用例期望不一致 |
| `test_template_decorator_metadata_is_registered` | 合成工站 `create_task` 的 `handles["output"]` 缺失，属合成工站上一轮改动的遗留 |

## 四、复位与停止

```powershell
# 停止工作区
unilab workspace stop --workspace . --wait 300 --json
```

虚拟工站在交互页点「初始化 / 清零」可清空库位与批次；重复测试前建议先清零再重新备料。
虚拟工站默认自动推进（对应整批实验）；只在需要手动单步验证时才加 `--no-auto-advance`
或在交互页取消勾选「自动推进」。

## 五、上真机前的注意事项

- 启动图 `ip` / `port` 改为合作方工站地址（界面常见 `8091`），并确认端口
- 不要开 `use_mock`
- `runtime-mode` 用 `normal`；`dry-run` 不连接真实驱动，通过 dry-run 不代表真机验收通过
- 真机首跑前确认联锁、急停、取消与异常恢复路径
- 生产地址与密钥不得写入设备包，由运行环境注入

## 附：本次使用的稳定身份

| 名称 | 值 |
| --- | --- |
| 类型命名空间 | `community.yb_sse_devices` |
| 设备类型 | `community.yb_sse_devices.conductivity_station` |
| 设备实例 | `conductivity_station_01` |
| 工作流 UUID | `3c8f1a62-9b74-4e2d-a5c1-7d0e8f4b2196` |
| 节点 UUID | `9d2e6c11-4a83-4f70-b1c5-0e8a37d5f4a2` |
| 本次任务 UUID | `21efa6b9-6753-461f-af5b-14531995d3dc` |
| 本次批次号 | `20260916123404106` |
