# 合成工站使用说明

面向现场联调和日常使用。协议字段见 [合成工站下单软件接口.md](合成工站下单软件接口.md)。

UniLab 是 TCP 客户端，固定加样下单软件（真机或虚拟机）是 TCP 服务端。报文为 UTF-8 JSON，CRLF 分帧。动作一律由 UniLab edge 下发，不要绕过 UniLab 直接改工站状态。

设备图独立于电导工站，路径为 `d:\Uni-Lab-OS\synthesis_station.json`。

## 设备与状态

注册 ID：`synthesis_station`。Deck：`SynthesisStation_Deck`，背景图文件名 `synthesis_station.webp`。本阶段 Deck **只有背景和状态镜像，没有槽位**。

查询动作：`query_tasks`、`query_posts`、`station_status`、`query_lot`、`query_bottle_code`、`query_upload_cubic_status`、`get_recipt_status`、`get_acoustic_resonance_status`、`get_sintering_status`、`test_connection`。查询失败时返回完整响应（看 `result`），不抛异常。

下单动作：`upload_recipe`、`create_task`、`start_task`、`upload_cubic`、`close_cabin_outer_door`、`confirm_recipe`、`start_recipt`、`start_acoustic_resonance`、`fetch_acoustic_resonance`、`finish_acoustic_resonance`、`scan_big_cubic_to_bottle`、`start_sintering`、`fetch_joule_heating`、`fetch_furnace`。写操作失败时 UniLab 应报错（`result ≠ 0`）。

工作流里不要用对象列表（`materials` / `slots` 前端展不开）。改成四列 / 两列基本类型，点添加后每项都是可填的文本或数字：

`upload_recipe`

- **物料名称** `powder_names`：`Li2S`、`LiBr`、`LiCl`、`P2S5`
- **物料重量 g** `powder_weights`：`0.9`、`0.87`、`1.2`、`0.88`
- **加样精度 g** `powder_tolerances`：`0.0007`、`0.0005`、`0.001`、`0.0005`
- **是否预加** `powder_pre_adds`：否、是、否、否

四列按顺序对齐。全部留空则用上面这组演示物料。

`create_task`

- **槽位号** `task_slot_nums`：`1`、`2`
- **槽位配方名称** `task_recipe_names`：两条都填 `260708-Li6PS5Cl-R1`

改完后必须 `--upload_registry`，并删掉旧节点重新拖入。旧节点还叫「物料列表 / 槽位列表」就说明没换到新 schema。驱动仍接受旧参数名 `materials` / `slots`，避免旧节点直接报 unexpected keyword。

`task_id` / `post_id` 由工站在 `create_task`（或后处理生成 POST）时分配，不必手填。工作流把 `create_task` 的 TASK ID 输出连到后续动作的 TASK ID 输入即可一路传到结束声共振。参数留空时：有连线用上游值，没有连线则用当前运行中的 TASK/POST。

状态 topic：`status` / `station_health` / `connected`；九路设备（`robot_arm`、`scanner`、`bead_device`、`acoustic_resonance`、`joule_heating`、`furnace_01`–`furnace_04`）；任务摘要 `running_task_count`、`running_post_count`、`current_task_id`、`current_post_id`。

看板以设备 topic 为准。Deck `unilabos_extra` 会镜像一份状态，不能替代 topic。

## 设备图字段

| 字段 | 含义 |
|---|---|
| `ip` / `port` | 工站 TCP 地址 |
| `connect_timeout` / `response_timeout` | 连接与等待响应超时 |
| `status_poll_interval` | 刷新状态并镜像到 Deck 的秒数，默认 30，`0` 关闭 |
| `use_mock` | `true` 时在本进程拉起虚拟机，真机不要开 |

## 图标

`unilab --upload_registry` 只上传注册表元数据，不会自动上传图片。把仓库内 [assets/synthesis_station.webp](assets/synthesis_station.webp) 放到 OSS `media/device_icon/`（或现场 CDN），再带 `--upload_registry` 启动一次。Deck 背景不显示时，先查文件名和这次上传。有正式渲染图时替换同名文件即可。

## 本地验证

在 `YB_SSE_Devices_Package` 目录：

```bash
unilab --check_mode --devices ./yb_sse_devices --external_devices_only
python -m pytest tests -q
```

## 虚拟机

默认 TCP `127.0.0.1:19101`，交互页 `http://127.0.0.1:19102/`。与电导虚拟机（`19091` / `19092`）端口不同，可以同时开。

```bash
python -m yb_sse_devices.synthesis_mock_server --host 127.0.0.1 --port 19101
```

`--demo` 预装示例配方 `260708-Li6PS5Cl-R1`、TASK-000168、POST-000198、LOT-260813-001。交互页可拨设备状态、装入演示、初始化清零、开关自动推进。

设备图填同一地址后启动 UniLab：

```bash
unilab --devices ./yb_sse_devices --external_devices_only -g ../synthesis_station.json
```

不要第二终端时，设备图设 `use_mock: true`。进程内 Mock **没有**交互页。

典型联调：`upload_recipe` → `create_task` → `start_task` → `query_tasks` / `query_posts` → `query_lot` → `query_bottle_code`。`create_task` 后 TASK 为未开始，必须再 `start_task` 才会就绪。虚拟机在 `start_task` 之后默认自动推进，完成后会生成 POST；`--no-auto-advance` 可关掉。

真机同样：`upload_recipe` → `create_task` → `start_task` → `upload_cubic` → （方舱时 `close_cabin_outer_door`）→ `confirm_recipe` → `start_recipt` → `start_acoustic_resonance` → `fetch_acoustic_resonance` → `finish_acoustic_resonance` → `scan_big_cubic_to_bottle` → `start_sintering` → `fetch_furnace` / `fetch_joule_heating`。

## 真机

1. 设备图 `ip` / `port` 填合作方工站。驱动默认端口 **8092 只是占位**，上线前向合作方确认。
2. 不要开 `use_mock`，不要连 `19101`。
3. 动作只从 UniLab 发。
4. Mock 自动推进 TASK→POST 仅为仿真便利，真机以现场软件为准。

常见错误码：`1` PLC 未连接，`2` 参数错误，`3` 找不到 LOT / 装瓶码，`4` 配方名重复，`5` 槽位超出托盘范围，`6` 找不到配方，`7` 找不到 TASK/POST，`8` 有别的 TASK 正在运行，`9` 状态不允许，`10` 找不到物料，`11` 物料非预加，`12` 找不到大坩埚，`13` 没有要下的小坩埚。

## 排错

- `test_connection` 失败或 `connected` 为 false：核对设备图 IP/端口，确认工站或 Mock 在听。
- 下单动作绿勾但没效果：驱动对 `result ≠ 0` 会抛错；若仍无任务，看 Mock 交互页最近一条动作。
- Deck 没有背景：检查 `synthesis_station.webp` 是否上传 OSS，以及是否 `--upload_registry`。
- 和电导工站同时部署：两套设备图、两套端口，不要混用。
